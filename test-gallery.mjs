
import fetch from 'node-fetch';

async function test() {
  console.log('Use node-fetch if we had an admin token, but we don\'t. We can test with supabase directly to make sure it inserts.');
}
test();

